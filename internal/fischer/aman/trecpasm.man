# generated by trirec/trecpas.pl at 2024-05-22 13:25
A080248	trecpas	0		.multiply(k * (k + 1) / 2)		(k*(k+1)/2)*V+U,T(n,n)=1	T(n
# A103450	trecpas	0				U+V+binomial(n-2,k-1)	k-1)	for:T(n, 0) = 1
A127156	trecpas	0				V+U	for:n,k>=1
A134991	trecpas	0	.multiply(n + k + 1)	.multiply(k + 1)		(k+1)*V+(n+k+1)*U	for:starting indices n=0 and k=0 beginning for P(2,t) (as suggested by a formula of David Speyer on MathOverflow)
A134991	trecpas	0	.multiply(n + k - 1)	.multiply(k)		k*V+(n+k-1)*U	for:starting indices n=1 and k=1 of table (cf
A141688	trecpas	0	.multiply(Functions.FIBONACCI.z(2 * (n - k + 1)))	.multiply(Functions.FIBONACCI.z(2 * k))		Fibonacci(2*k)*V+Fibonacci(2*(n-k+1))*U,		for:T(n, 1) = T(n, n) = 1
A143409	trecpas	0		.multiply(n + k)		(n+k)*V+U
A162741	trecpas	0				U+V,T(n,1)=1and	T(n	for:n>1: T(n,2*n-2) = T(n,2*n-1)=1
A178522	trecpas	0				T(k,n)=T(k-1,n-1)+T(k-1,n)	n)=T(k-1	for:T(0,0)=1, T(k,0)=1 for k>0, T(0,n)=2 for n>0
A225372	trecpas	0	.multiply(-2 * n + 2*k + 1)	.multiply(-2*k + 3)		(m*n-m*k+1)*U+(m*k-m+1)*V,		for:T(n, 1) = T(n, n) = 1, and m = -2
A248925	trecpas	0	.multiply( - n)	.multiply((k == n ? 1 : 0) + (k < n ? 1 : 0))		[k==n]+[k<n]*V-n*U
A259456	trecpas	0	.multiply(n - k - 1)	.multiply(n - k - 1)			(n-k-1)*(U+V),n>=1,1<=k<=n	n>=1
A269939	trecpas	0	.multiply(n + k - 1)	.multiply(k)		k*V+(n+k-1)*U	for:n > 0, T(0,0) = 1, T(n,0) = 0 for n > 0
A276067	trecpas	0				V+U(n>=3,k>=2)	k>=2)
# A326659	trecpas	0	.multiply([0<k<=n] * n * ()			[0<k<=n]*n*(U+V)+[k=0andn>=0];triangleT(n,k),n>=0,0<=k<=n,readbyrows	triangleT(n
A336633	trecpas	0	.multiply(2 * n - k - 1)	.multiply(n + k)		(n+k)*V+(2*n-k-1)*U	for:positive integers n and 0 <= k < n; T(0,0)=1 (or T(1,0)=1); otherwise T(n,k)=0
# A350110	trecpas	0				V+U	for:n>=2*k+1 for k>=0
# A350111	trecpas	0				V+U	for:n>=3*k+1 for k>=0
# A350112	trecpas	0				V+U	for:n >= 4*k+1 for k >= 0
# A350512	trecpas	0				V+U+2*binomial(n-2,k-1)	k-1)	for:0 < k < n
# A354665	trecpas	0				V+U	for:n>=2*k+1 for k>=0
# A354666	trecpas	0				V+U	for:n>=3*k+1 for k>=0
# A354667	trecpas	0				V+U	for:n>=4*k+1 for k>=0
# A354668	trecpas	0				V+U	for:n>=4*k+1 for k>=0
A371080	trecpas	0		.multiply(3 * (n - 1) + k)		U+(3*(n-1)+k)*V	for:0 < k < n for initial values T(n, 0) = 0 for n > 0 and T(n, n) = 1 for n >= 0
A371081	trecpas	0		.multiply(Z.valueOf(n + k - 1).square())		U+(n+k-1)^2*V
A371259	trecpas	0		.multiply(Z.valueOf(n + k - 1).pow(2))		U+(n+k-1)^2*V
A371277	trecpas	0		.multiply(Z.valueOf(n + k - 1).pow(3))		U+(n+k-1)^3*V
A372208	trecpas	0		.multiply(Z.valueOf(n + k - 1).pow(3))		U+(n+k-1)^3*V
